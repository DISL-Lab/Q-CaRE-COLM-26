# Q-CARE dependencies.
# The pinned versions are the ones used to produce the results in the paper.
# Greedy decoding + these pinned versions make the pipeline deterministic
# for a fixed backbone and GPU.

torch==2.11.0
transformers==5.7.0
accelerate==1.13.0
numpy==2.2.6
tqdm==4.67.3
safetensors==0.7.0

# Prompt loading. Not numerics-critical, so a floor rather than a pin.
PyYAML>=5.4

# Correlation statistics for analysis/human_agreement.py and
# analysis/retriever_comparison.py. Not needed to run an evaluation.
scipy>=1.10
